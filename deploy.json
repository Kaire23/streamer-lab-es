{
  "name": "yostreamer-nextjs-app",
  "description": "Next.js SSR application for streaming equipment guides",
  "type": "nodejs",
  "build": {
    "command": "node next-build.cjs",
    "env": {
      "NODE_ENV": "production"
    }
  },
  "start": {
    "command": "node next-start.cjs",
    "env": {
      "NODE_ENV": "production",
      "PORT": "${PORT:-5000}"
    }
  },
  "requirements": {
    "node": ">=18.0.0",
    "directories": [".next"],
    "files": ["next.config.js", "package.json"]
  },
  "notes": [
    "This is a Next.js SSR application",
    "Build creates .next directory with optimized production assets",
    "Start command uses Next.js native production server",
    "No custom server wrapper - uses Next.js directly"
  ]
}