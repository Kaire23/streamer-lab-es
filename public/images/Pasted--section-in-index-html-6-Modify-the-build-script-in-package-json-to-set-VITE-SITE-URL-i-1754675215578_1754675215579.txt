` section in \"index.html.\"
6. Modify the build script in \"package.json\" to set `VITE_SITE_URL` in production.
7. Verify results by checking the dist directory after building.
8. Optionally configure sitemap generation for matching slugs.

We might also want to prerender the `/post/:slug` route for redundancy. Here's a test plan to validate:
- Check if a sample route's static HTML contains the correct streamer title.
- Ensure open graph metadata includes absolute canonical URLs. 

Now I’ll wrap this in a concise response!" duration="6.331060">

¡Perfecto! Vamos a implementar el prerender (SSG) con Vite para que Google y otros bots vean HTML estático completo en “/” y en todas las páginas “/setup/*”, manteniendo tu SPA, Analytics y el fallback. Además, ajusto el SEO para que las URLs canónicas y los JSON‑LD sean absolutas en build (no dependan de window).

Puntos clave que cubre el plan:
- Prerender automático de rutas: homepage y todas las de setup, descubiertas por crawling desde “/”.
- Mantener la navegación SPA y el tracking de GA en hidratación (ya lo tienes con RouteChangeTracker).
- Metadatos SEO correctos en HTML estático (canónico y og:image absolutos con VITE_SITE_URL).
- Sin cambios en tu arquitectura actual; solo añadimos un plugin y pequeños ajustes.

lov-plan
Plan para SSG con Vite (prerender) y SEO absoluto

1) Dependencias y configuración
- Añadir devDependency: vite-prerender-plugin (plugin agnóstico de framework y mantenido).
- package.json: mantener "build": "vite build". No hay scripts extra.

2) Vite: activar prerender
- Editar vite.config.ts:
  - Importar el plugin de prerender.
  - Activarlo solo en build y con crawling:
    - entryRoutes: ["/"] para arrancar el crawler en la home.
    - enableCrawl: true para seguir enlaces internos  y generar /setup/* en dist.
    - (Opcional) include/exclude para filtrar, pero no es necesario dado que la home lista todas.
  - Mantener el resto de tu configuración (plugin react, alias, etc.).

3) SEO absoluto en build (canónica/OG/JSON-LD)
- Editar src/components/SEO.tsx:
  - Usar import.meta.env.VITE_SITE_URL (por ejemplo, https://yostreamer.com) como base cuando window no existe (build/prerender).
  - canonical = new URL(canonicalPath, siteUrl).toString()
  - og:image y twitter:image también con URL absoluta basada en siteUrl.

- Editar src/pages/Index.tsx y src/pages/PostPage.tsx:
  - Donde ahora usan window.location.origin en JSON-LD, cambiar a usar siteUrl (import.meta.env.VITE_SITE_URL || window.origin en cliente). Así los JSON‑LD quedan con URLs absolutas también en prerender.

4) Variables de entorno
- Crear .env.production con VITE_SITE_URL=https://yostreamer.com (o configurarlo en variables del proyecto). No afecta dev.

5) Verificación y QA
- Ejecutar build: se generarán HTML estáticos en dist/, incluyendo:
  - dist/index.html
  - dist/setup/ibai-llanos/index.html, etc.
- Verificar que:
  - El HTML de cada ruta contiene el contenido de la guía (texto e imágenes).
  -  y og:image son absolutos.
  - Google Analytics sigue funcionando tras la hidratación SPA (ya existente con RouteChangeTracker).
- Mantener sitemap.xml y robots.txt actuales (apuntan a /setup/*, ya prerenderizadas).
fin lov-plan

¿Quieres que lo aplique ahora? Esto te dará HTML estático indexable hoy mismo y solucionará el problema de “contenido no accesible para bots” sin migrar a Next.js.

Implement the plan